"""验证方式说明 - 静态提示词"""


VERIFICATION_METHODS = """## 验证方式说明

执行动作后，系统会根据你指定的验证方式验证操作是否成功。请根据动作类型选择合适的验证方式。

### 1. accessibility（无障碍树 + 聚焦对比）

**适用场景**：
- 快捷键操作（hotkey）
- 文本输入（type）
- 窗口切换、焦点变化
- 系统操作（打开/关闭窗口、创建对话框）

**验证流程**：
1. 执行动作前同时采集无障碍树（depth=15）和聚焦元素
2. 执行动作后获取新的无障碍树和聚焦元素
3. 对比两个维度的变化：
   - 聚焦元素：role/name/bounds/state/value 前后变化
   - 无障碍树：元素新增/删除/修改的结构差异
4. 将两个维度的变化 + 验证标准发送给 LLM 综合分析
5. 返回验证结果

**何时选择**：
- 预期有明确的 UI 结构变化（新窗口、新对话框、焦点转移）
- 操作结果可通过无障碍树元素变化体现
- 快捷键/焦点转移等操作（聚焦变化是主信号）
- 不需要视觉外观确认

**示例**：
```json
{{
  "action": "hotkey(key='ctrl o')",
  "verification": {{
    "method": "accessibility",
    "prompt": "文件打开对话框是否出现"
  }}
}}
```

---

### 2. visual（视觉验证）

**适用场景**：
- 点击按钮后需要视觉确认（如保存成功的提示）
- 界面视觉状态变化（如按钮高亮、弹窗动画）
- 无障碍树无法捕捉的视觉效果（toast 提示、加载动画）
- 需要确认操作"看起来"成功

**验证流程**：
1. 执行动作后截图
2. 将截图 + 验证标准发送给视觉模型
3. 视觉模型判断是否满足验证标准
4. 返回验证结果

**何时选择**：
- 操作结果主要是视觉变化（颜色、文本、图标）
- 无障碍树对比无法确认成功（如 toast 提示）
- 关键的确认步骤（保存、删除等不可逆操作）

**示例**：
```json
{{
  "action": "click(point='<point>600 400</point>')",
  "verification": {{
    "method": "visual",
    "prompt": "确认保存成功的绿色提示框是否出现在屏幕中央"
  }}
}}
```

---

### 3. mixed（混合验证）

**适用场景**：
- 关键步骤、不确定的结果
- 无障碍树对比不明确时追加视觉验证
- 需要双重确认的复杂操作

**验证流程**：
1. 先执行无障碍树对比验证
2. 如果树有变化且 LLM 判断成功 → 返回成功
3. 如果树无变化或 LLM 不确定 → 追加视觉验证
4. 视觉验证结果作为最终结论

**何时选择**：
- 操作可能产生轻微变化（树对比可能检测不到）
- 无法确定选择 accessibility 还是 visual
- 重要操作（保存文件、提交表单）

**示例**：
```json
{{
  "action": "click(point='<point>500 500</point>')",
  "verification": {{
    "method": "mixed",
    "prompt": "确认提交按钮是否被点击，表单是否提交成功"
  }}
}}
```

---

### 验证方式选择指南

| 动作类型 | 推荐验证方式 | 原因 |
|---------|------------|------|
| hotkey | accessibility | 快捷键通常改变焦点或打开对话框 |
| type | accessibility | 输入后焦点元素或值会变化 |
| click (打开) | accessibility | 新窗口/对话框会在树中体现 |
| click (确认) | visual/mixed | 需要确认操作效果 |
| scroll | accessibility | 窗口内容变化会在树中体现 |
| drag | mixed | 可能涉及位置和内容变化 |
| check_input | accessibility | 检查结果会体现在树中 |

---

### 验证标准编写建议

好的验证标准应该：
1. **具体明确**: "确认对话框出现" > "操作成功"
2. **可观察**: 描述你能看到或可检测到的变化
3. **与动作相关**: 直接对应你执行的动作的预期结果

**示例**：
- ✓ "浏览器窗口已打开并显示主页"
- ✓ "保存对话框已关闭，焦点返回编辑器"
- ✓ "屏幕中央出现绿色的保存成功提示"
- ✗ "操作完成"（太模糊）
- ✗ "应该成功了"（主观）
"""


__all__ = ["VERIFICATION_METHODS"]