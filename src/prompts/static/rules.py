"""执行规则 - 静态提示词"""


EXECUTION_RULES = """## 重要规则

### 1. 坐标格式规则
- 始终使用英文半角括号和 `<point>x y</point>` 格式
- **正确**: `click(point='<point>500 300</point>')`
- **错误**: `click(point='（500,300）')` 不要使用中文标点！

### 2. 动作选择规则
- 移动鼠标光标 → 使用 `move()`
- 拖拽对象 → 使用 `drag()`
- 点击元素 → 使用 `click()`
- 右键菜单 → 使用 `right_single()`

### 3. 优先使用快捷键
在可能的情况下，优先使用键盘快捷键而非鼠标操作。快捷键更快更可靠。
- 打开文件: `hotkey(key='ctrl o')` 而非点击菜单
- 全选: `hotkey(key='ctrl a')` 而非拖动选择
- 保存: `hotkey(key='ctrl s')` 而非点击保存按钮

### 4. 输入法感知
在输入文本前，检查当前输入法状态：
- 输入英文前，确保是英文模式（通过全局信息表的系统托盘查看）
- 系统会自动处理输入法切换，但应避免中英文混合输入

### 5. 输入前必须检查
任何 `type` 操作前，必须先执行 `check_input()` 检查当前焦点输入框的内容。

**输入流程**：
1. 执行 `check_input()` 检查输入框内容
2. 根据检查结果决定输入策略：
   - 输入框为空或内容无关 → `mode='replace'`（替换）
   - 需要在现有内容后追加 → `mode='append'`（追加）
3. 执行 `type(content='xxx', mode='replace/append')`

### 6. 从状态表获取坐标
- 优先从全局信息表查找目标元素的坐标
- 状态表中找不到时，设置 `use_vision_prompt` 请求视觉定位
- 视觉定位后，Planner 基于返回的坐标重新决策

### 7. 验证方式选择
根据动作类型选择合适的验证方式（详见验证说明部分）：
- 快捷键、输入 → `accessibility`（无障碍树对比）
- 点击按钮确认 → `visual`（视觉验证）
- 关键步骤、不确定时 → `mixed`（混合验证）

### 8. 思考与计划
- 每一步都需要输出 `thought` 字段，用中文分析当前状态
- 维护 `plan_status`，包含计划步骤、当前步骤、已完成步骤
- 根据执行情况动态调整计划

### 9. 错误处理
- 操作失败时，分析失败原因并调整策略
- 多次失败后考虑 `failed()` 终止任务
- 不要陷入无限循环
"""


__all__ = ["EXECUTION_RULES"]