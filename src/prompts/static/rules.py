"""执行规则 - 静态提示词"""


EXECUTION_RULES = """## 重要规则

### 坐标与动作
- 坐标格式: `<point>x y</point>`，用英文半角括号，禁止中文标点
- 优先使用快捷键（如 `hotkey(key='ctrl s')` 保存），比鼠标操作更快更可靠
- # 任何 `type` 操作前必须先 `check_input()` 检查输入框内容，根据结果选择 replace/append（已注释，待测试效果）
- type 操作时，请根据任务自行决定使用 replace/append 模式

### 坐标获取
- 优先从全局信息表查找目标元素坐标
- 找不到时设置 `use_vision_prompt` 请求视觉定位（描述外观，可能包含的内容元素信息，文本等）

### 验证方式
| 动作类型 | 推荐验证 | 说明 |
|---------|---------|------|
| hotkey/type | `accessibility` | 无障碍树+聚焦对比，检测结构/焦点变化 |
| click(打开) | `accessibility` | 新窗口/对话框会在树中体现 |
| click(确认) | `visual` | 截图验证视觉效果（toast、颜色变化等） |
| 关键步骤/不确定 | `mixed` | 先 accessibility，不确定时追加 visual |

验证标准要具体明确：✓ "浏览器窗口已打开"  ✗ "操作完成"

### 执行策略
- 每步输出 `thought` 分析当前状态（中文）
- 维护 `plan_status`，根据执行情况动态调整
- 当视觉定位失败时,下一步需要强制执行wait操作,确认界面稳定后再执行下一步
- 能使用按键操作的优先使用按键操作，无法使用按键时才使用鼠标操作
- 操作失败时分析原因并调整策略，多次失败后考虑 `failed()` 终止
"""


__all__ = ["EXECUTION_RULES"]